public String formatName(String firstName, String lastName) {
    
String formattedName = (String.isBlank(lastName) ? '' : lastName) + ', ' + (String.isBlank(firstName) ? '' : firstName);
    return formattedName;
}
}