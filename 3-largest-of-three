public static Integer findLargest(Integer num1, Integer num2, Integer num3) {
    Integer[] numbers = new Integer[]{num1, num2, num3};  
    numbers.sort();
    Integer bigNum = numbers[numbers.size() - 1];
    return bigNum;
}