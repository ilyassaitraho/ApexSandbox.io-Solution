public Boolean isPrime(Integer num) {
    if (num == null || num < 2) 
        return false;
    for (Integer i = 2; i < num; i++) {
        if (Math.mod(num, i) == 0) {
            return false;  
        }
    }
    return true;  
}