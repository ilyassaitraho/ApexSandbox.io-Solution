public String nameFromEmail(String email) {
    Integer middleindex = email.indexOf(.);
    Integer secondIndex = email.indexOf(@);
    String firstName = email.substring(0,middleindex) ;
    String lastName = email.substring(middleindex , secondIndex ) ; 
    return firstName+' '+lastName;
}